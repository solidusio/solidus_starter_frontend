<div class="products">
  <aside>
    <%= render 'spree/shared/navigation/taxonomies', taxon: @taxon %>
    <%= render 'spree/shared/search/filters', taxon: @taxon if @taxon.children.empty? %>
  </aside>
  <section>
    <h1 class="products__taxon-title"><%= @taxon.name %></h1>

    <%= render 'spree/products/products', products: @products, taxon: @taxon %>

    <% unless params[:keywords].present? %>
      <% cache [I18n.locale, @taxon] do %>
        <%= render partial: 'spree/products/products_by_taxon', collection: @taxon.children %>
      <% end %>
    <% end %>
  </section>
</div>
