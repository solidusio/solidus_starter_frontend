#!/usr/bin/env bash

set -e

case "${DB}" in
  postgres|postgresql)
    railsdb="postgresql"
    host="${DB_POSTGRES_HOST:-${DB_HOST}}"
    username="${DB_USERNAME}"
    password="${DB_PASSWORD}"
    ;;
  mysql)
    railsdb="mysql"
    host="${DB_MYSQL_HOST:-${DB_HOST}}"
    username="${DB_USERNAME}"
    password="${DB_PASSWORD}"
    ;;
  sqlite|'')
    railsdb="sqlite3"
    ;;
  *)
    echo "Invalid DB specified: ${DB}"
    exit 1
    ;;
esac

if [[ -n "${SOLIDUS_BRANCH}" ]]; then
  branch="${SOLIDUS_BRANCH}"
else
  branch="master"
fi

extension_name="solidus_starter_frontend"
sandbox_name='sandbox'
extension_gem_line=$(cat templates/gemfiles/090_sandbox_solidus_starter_frontend.rb)
sandbox_path="./${sandbox_name}"

if [[ -n "${SKIP_SPECS}" ]]; then
  rspec_rails_gem_line=''
else
  rspec_rails_gem_line=$(cat templates/gemfiles/100_rspec_rails.rb)
fi

if [[ "${SANDBOX_SKIP_AUTHENTICATION}" == '1' ]]; then
  solidus_auth_devise_gem_line=''
  solidus_install_user_class_argument=""
else
  solidus_auth_devise_gem_line=$(cat templates/gemfiles/060_solidus_auth_devise.rb)
  solidus_install_user_class_argument="--user_class=Spree::User"
fi

rm -rf "${sandbox_path}"
bundle exec rails new "${sandbox_name}" --database="${railsdb}" \
  --skip-bundle \
  --skip-git \
  --skip-keeps \
  --skip-rc \
  --skip-spring \
  --skip-test \
  --skip-javascript

if [[ ! -d "${sandbox_name}" ]]; then
  echo 'sandbox rails application failed'
  exit 1
fi

cd "${sandbox_path}"

cat ../templates/gemfiles/040_solidus.rb \
  ../templates/gemfiles/050_solidus_i18n.rb >> Gemfile

cat >> Gemfile <<RUBY
${extension_gem_line}
${solidus_auth_devise_gem_line}

${rspec_rails_gem_line}
RUBY

replace_in_database_yml() {
  if [[ "${railsdb}" == "postgresql" ]]; then
    sed -i.bck "/^  adapter:/a \ \ $1:  $2" config/database.yml
  elif [[ "${railsdb}" == "mysql" ]]; then
    sed -i.bck "s/^  $1:.*/\ \ $1: $2/" config/database.yml
  fi

  if [[ -f config/database.yml.bck ]]; then
    rm -f config/database.yml.bck
  fi
}

if [[ -n "${host}" ]]; then
  replace_in_database_yml "host" "${host}"
fi

if [[ -n "${username}" ]]; then
  replace_in_database_yml "username" "${username}"
fi

if [[ -n "${password}" ]]; then
  replace_in_database_yml "password" "${password}"
fi

if [[ -n "${SANDBOX_BUNDLE_PATH}" ]]; then
  bundle config --local path "${SANDBOX_BUNDLE_PATH}"
fi

bundle install --gemfile Gemfile

bundle exec rake db:drop db:create

bundle exec rails generate solidus:install \
  --auto-accept \
  "${solidus_install_user_class_argument}" \
  --enforce_available_locales=true \
  --with-authentication=false \
  --payment-method=none \
  "$@"

if [[ "${SANDBOX_SKIP_AUTHENTICATION}" != '1' ]]; then
  bundle exec rails generate solidus:auth:install --auto-run-migrations
fi

bundle exec "${extension_name}" --auto-accept

echo
echo "🚀 Sandbox app successfully created for ${extension_name}!"
echo "🚀 Using ${railsdb} and Solidus ${branch}"
echo "🚀 Use 'export DB=[postgres|mysql|sqlite]' to control the DB adapter"
echo "🚀 Use 'export SOLIDUS_BRANCH=<BRANCH-NAME>' to control the Solidus version"
echo "🚀 This app is intended for test purposes."
